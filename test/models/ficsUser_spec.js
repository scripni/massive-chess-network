var assert = require("assert");
var FicsUser = require("../../lib/models/ficsUser");

describe("A FICS user", function() {
	describe("A successful anonymous connection", function() {
		it("signs in receiving a Guest**** username");
		it("signs out if the connection is terminated");
	});
});
